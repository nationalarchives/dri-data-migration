prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>
prefix dri: <http://nationalarchives.gov.uk/terms/dri#> 

select ?legislation ?label where {
    bind(@id as ?legislation)
    optional { ?legislation rdfs:label ?label }
}
