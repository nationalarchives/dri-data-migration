namespace Api;

public interface IDriRecord
{
    string Id { get; init; }
    /*
    void Deconstruct(out string Id);
    bool Equals(DriRecord? other);
    bool Equals(object? obj);
    int GetHashCode();
    string ToString();*/
}