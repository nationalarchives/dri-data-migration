prefix ex: <http://id.example.com/schema/>

construct {
    ?s ex:inquiryEvidenceProviderName ?inquiryEvidenceProviderName.
} where {
    bind(@id as ?inquiryEvidenceProviderName)
    ?s ex:inquiryEvidenceProviderName ?inquiryEvidenceProviderName.
}